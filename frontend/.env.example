# 前端 API 後端網址
# 開發環境:預設 fallback 為 http://localhost:3001(見 src/services/api.ts),
# 此設定僅供本機開發參考,非必要。
# 正式環境部署到 Render Static Site 時,請在 Render dashboard 的
# 「Environment」設定此變數為實際後端服務網址(build time 注入),
# 覆蓋本檔案內容。
VITE_API_BASE_URL=http://localhost:3001
